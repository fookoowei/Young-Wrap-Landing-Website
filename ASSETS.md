# Assets

## 3D model
- `public/models/car.glb` — "Sports Car" by Quaternius (CC0 / public domain), from the Cars Bundle on Poly Pizza: https://poly.pizza/m/1mkmFkAz5v (bundle: https://poly.pizza/bundle/Cars-Bundle-FE5IWe6OMk). 171 KB.

## Supplied by owner (pending — placeholders in use)
- Opening hours (phone ✓, address ✓ done) → `src/config.js` and JSON-LD in `index.html`
- Logo + brand colors → `--brand-accent` in `src/styles/main.css`
- Gallery photos → replace `public/images/gallery-*.svg`
- Charles Tee collab post URL → `SHOP.kol.postUrl` in `src/config.js`
